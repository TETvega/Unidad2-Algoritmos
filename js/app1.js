const autenticado = true;
const puedePagar= false;



// Operadores Ternarios
console.log(autenticado ? `Usuario no autenticado ` : `Usuario no autenticado`);
console.log(autenticado && puedePagar ? `Usuario no autenticado, puede pagar ` : `Usuario no autenticado, no puedepagar`);

